{{#if checkboxes}}
	<h2 class="title">{{ title }}</h2>
	<div class="remove-user-users">
		{{#checkboxes}}
			{{{.}}}
		{{/checkboxes}}
	</div>
	{{#if noUsersError}}{{{ noUsersError }}}{{/if}}
	{{{ deleteButton }}}
{{else}}
	Нет доступных для удаления пользователей
{{/if}}
